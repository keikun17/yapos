%table.table.table-striped
  %thead
    %tr
      %th= model_class.human_attribute_name(:quote_reference)
      %th= model_class.human_attribute_name(:client)
      %th= model_class.human_attribute_name(:order_reference)
      %th= model_class.human_attribute_name(:quantity)
      %th= model_class.human_attribute_name(:description)
      %th= model_class.human_attribute_name(:supplier)
      %th= model_class.human_attribute_name(:status)
      %th= model_class.human_attribute_name(:quote_date)
      %th=t '.actions', :default => t("helpers.actions")
  %tbody
    - quotes.each do |quote|
      %tr
        %td= link_to quote.quote_reference, quote_path(quote)
        %td
          = link_to quote.client_name, client_path(quote.client) if quote.client
        %td
          = link_to quote.order_reference, order_path(quote.order) if quote.order
        %td= quote.quantity
        %td= truncate(quote.description, :length => 20, :omission => "...")
        %td
          = link_to quote.supplier_name, supplier_path(quote.supplier)
        %td= quote.display_status
        %td
          = quote.quote_date.to_date if quote.quote_date
        %td
          = link_to t('.edit', :default => t("helpers.links.edit")), edit_quote_path(quote), :class => 'btn btn-mini'
          = link_to 'Requote', requote_quote_path(quote),  :class => 'btn btn-mini'
          = link_to t('.destroy', :default => t("helpers.links.destroy")), quote_path(quote), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-mini btn-danger'


