.modal.fade.vendor_code_modal
  .modal-dialog
    .modal-content
      .modal-header
        %a.close.data{data: {dismiss: 'modal'}, 'aria-hidden' => true} x

      .modal-body

        - if vendor_item.present?
          .col
            .row
              %h3  Current Values
              %p
                %strong Product : #{vendor_item.product.name}
              %p
                %strong Vendor Code : #{vendor_item.csv}

        .row
          .form-group
            = label_tag :product_id, 'Product', class: 'control-label col-sm-3'
            .col-sm-5
              -# FIXME : tb_render_form: true is ugly
              = select_tag :product_id, options_for_select(Product.all.collect{|p| [p.name , p.id] }), include_blank: true, tb_render_form: true , tb_render_form_to: '#vendor_item_form', class: 'form-control'


        .vendor_item_form
          -# = form_for VendorItem.new.initialize_fields(product), remote: true, html: {class: 'form-horizontal'} do |f|3
          -#
          -#   .modal-body
          -#     .form-group
          -#       = f.label :code, 'Vendor Item Code', class: 'control-label'
          -#       .controls
          -#         = f.text_field :code, class: 'col-sm-6 text_field'
          -#
          -#   .modal-footer
          -#     -# %button.btn{data: {dismiss:"modal"}, 'aria-hidden' => true} Close
          -#     = f.submit nil, :class => 'btn btn-primary', remote: true
